# Override Ubuntu's default (D /tmp ...), which empties /tmp at boot. That races
# non-systemd users of the system that also run at boot. Lowercase d keeps the
# age-based cleanup but does not wipe /tmp. (Borrowed from boldsoftware/exeuntu.)
d /tmp 1777 root root 30d
