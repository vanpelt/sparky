# sparkbox host networking knobs, applied on every boot.
#   ip_forward       route sandbox traffic between taps and the uplink.
#   v6 forwarding    with --subnet6 each sandbox gets a routable /128 and
#                    egresses without NAT (harmless to enable when unset).
#   rp_filter strict the metadata service identifies a caller by source address
#                    and the host forwards between taps, so a guest must not be
#                    able to source-spoof a neighbour.
net.ipv4.ip_forward=1
net.ipv6.conf.all.forwarding=1
net.ipv4.conf.all.rp_filter=1
net.ipv4.conf.default.rp_filter=1
