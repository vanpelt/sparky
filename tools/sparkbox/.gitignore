# local `go build ./cmd/sparkbox` output (README quick-start), never committed
/sparkbox
# local mock-run state dir (`--state-dir ./state`)
/state/
# local dev environment: working-tree binaries, gateway state, fleet private
# keys minted by hack/dev/gateway.sh (see hack/dev/README.md)
/.dev/
